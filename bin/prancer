#!/usr/bin/env perl6

use v6;
use App::Prancer::Handler;

#multi GET( '/foo' ) is route { }
multi GET( '/a', '/d' ) is route { }
multi GET( '/b' ) is route { }
multi GET( '/c' ) is route { }

#`(
#
# The equivalent of **, *@rest captures the path and puts it into @rest.
#
multi GET( '/', @rest ) is route { return "Everything else, @rest" }

#
# The equivalent of '*', $value with no constraints matches any value.
#
multi GET( '/', $value ) is route
	{ return "/$value that's not declared elsewhere" }
multi GET( '/', Str $slug ) is route
	{ return "/$slug is a string that doesn't fall into \$value catchall" }
multi GET( '/', Int $id ) is route
	{ return "/$id is an int that doesn't fall into \$value catchall" }

multi GET( '/regex', Str $x where { /foo/ } ) is route { }

#
# Blog route from PearlBee (corrected) in Prancer form
#
multi GET( '/' ) is route { }
multi sub GET( '/page', Int:D $page ) is route { }
multi sub GET( '/post', Str:D $slug ) is route { }
multi sub GET( '/post', Int:D $year, Int:D $month, Str:D $slug ) is route { }
multi POST( '/comment', '/add', %QUERY ) is route { }

#
# Ho ho ho, can we use destructuring bind here?
#
# multi POST( '/comment', '/add', [ $fullname, $slug, $secret ] ) is route { }

multi GET( '/posts/category', Str:D $slug ) is route { } # Randomly elide '/'
multi GET( '/posts', '/category', Str:D $slug, '/page', Int:D $page )
         is route { }
multi GET( '/posts/user', Str:D $username ) is route { } # Randomly elide '/'
multi GET( '/posts', '/user', Str:D $username, '/page', Int:D $page )
         is route { }
multi GET( '/posts/tag', Str:D $slug ) is route { } # Randomly elide '/'
multi GET( '/posts', '/tag', Str:D $slug, '/page', Int:D $page )
         is route { }
multi GET( '/sign-up' ) is route { }
multi POST( '/sign-up', %QUERY ) is route { }
#
#
# multi POST( '/sign-up', [ $username, $email, $first-name, $last-name ] )
#           is route { }

multi GET( '/api', '/tags', $format ) is route { }
multi GET( '/admin' ) is route { }
multi POST( '/login', %QUERY ) is route { }
#
#
# multi POST( '/login', [ $password, $username ] ) is route { }
#
multi GET( '/logout' ) is route { }

#
# Note /author/posts/* and /author/comments/* aren't quite parallel.
#
multi GET( '/author', '/posts' ) is route { }
multi GET( '/author', '/posts', Str:D $status, '/page', Int:D $page )
         is route { }
multi GET( '/author', '/posts', '/draft', Str:D $slug ) is route { }
multi GET( '/author', '/posts', '/edit', Str:D $slug ) is route { }
multi GET( '/author', '/posts', '/publish', Str:D $slug ) is route { }
multi GET( '/author', '/posts', '/trash', Str:D $slug ) is route { }
multi GET( '/author', '/posts', '/update', Str:D $slug ) is route { }
multi GET( '/author', '/posts', '/add' ) is route { }

multi GET( '/author', '/comments' ) is route { }
multi GET( '/author', '/comments', '/page', Int:D $page ) is route { }
multi GET( '/author', '/comments', Str:D $status, '/page', Int:D $page )
         is route { }
multi GET( '/author', '/comments', '/approve', Str:D $slug ) is route { }
multi GET( '/author', '/comments', '/trash', Str:D $slug ) is route { }
multi GET( '/author', '/comments', '/spam', Str:D $slug ) is route { }
multi GET( '/author', '/comments', '/pending', Str:D $slug ) is route { }

#
# Ding ding --- 'redirect' feature?
#
# multi redirect( '/admin', '/users' --> '/admin', '/users', '/page', 1 )
#               is route { }
multi GET( '/admin', '/users' ) is route { } # -> '/admin/users/page/1'
multi GET( '/admin', '/users', '/page', Int:D $page ) is route { }
multi GET( '/admin', '/users', Str:D $status, '/page', Int:D $page )
         is route { }
multi GET( '/admin', '/users', '/activate', Str:D $slug ) is route { }
multi GET( '/admin', '/users', '/deactivate', Str:D $slug ) is route { }
multi GET( '/admin', '/users', '/suspend', Str:D $slug ) is route { }
multi GET( '/admin', '/users', '/allow', Str:D $slug ) is route { }
multi GET( '/admin', '/users', '/add', Str:D $slug ) is route { }

# Another 'redirect' opportunity?
multi GET( '/admin/tags' ) is route { }
multi GET( '/admin/tags', Str:D $slug, '/page', Int:D $page ) is route { }
multi GET( '/admin', '/tags', '/delete', Str:D $slug ) is route { }
multi GET( '/admin/settings' ) is route { }
multi POST( '/admin/settings/save' ) is route { }
multi GET( '/admin/settings/import' ) is route { }
multi POST( '/admin/settings/wp_import' ) is route { }

# Redirect --> /admin/posts/$status/page/$page
multi GET( '/admin/posts' ) is route { }
multi GET( '/admin', '/posts', Str:D $status, '/page', Int:D $page )
         is route { }
multi GET( '/admin', '/posts', '/publish', Str:D $slug ) is route { }
multi GET( '/admin', '/posts', '/draft', Str:D $slug ) is route { }
multi GET( '/admin', '/posts', '/trash', Str:D $slug ) is route { }
multi GET( '/admin', '/posts', '/update', Str:D $slug ) is route { }
multi GET( '/admin', '/posts', '/edit', Str:D $slug ) is route { }
multi GET( '/admin/posts', '/add' ) is route { }

multi GET( '/admin', '/tags', '/edit', Str:D $slug ) is route { }

# XXX Coalesce these into 'any'?
#
multi GET( '/dashboard' ) is route { 'Dashboard' }
multi POST( '/dashboard' ) is route { }

# XXX Coalesce these into 'any'?
#
multi GET( '/profile' ) is route { }
multi POST( '/profile' ) is route { }

multi GET( Str $x ) is route { "Caught $x" }
)

multi MAIN( 'build-skeleton', Str:D $app-name )
	{
say "Building skeleton...";
	}

multi MAIN( 'run-blog', Str:D $app-name,
                        Str:D $admin-username,
                        Str:D $admin-password )
	{
say "Building blog...";
say "Launching blog...";
	prance;
	}

multi MAIN( 'run-skeleton', Str:D $app-name,
                            Str:D $admin-username,
                            Str:D $admin-password )
	{
say "Building skeleton...";
say "Launching skeleton...";
	}

# vim: ft=perl6
